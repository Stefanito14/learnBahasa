# Mode « Challenge 75 » — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter un mode « Challenge 75 » gamifié (5 piliers/jour, streak, XP, anneaux, grille 75, badges) inspiré du 75 Fluent Challenge, pour pousser à pratiquer chaque jour.

**Architecture:** Un module pur `challenge-core.js` (patron UMD, testé avec `node:test`) calcule tout à partir d'un état sérialisé stocké sous la clé Store `challenge`. `index.html` charge le module, branche les hooks d'activité existants (`lessonDone`, `srsUpdate`, quiz écoute) sur `markPillar()`, et rend l'onglet « Challenge » + un mini-encart sur l'accueil. Aucun compteur persisté n'est dupliqué : streak, XP, progression et badges sont dérivés par calcul.

**Tech Stack:** JavaScript vanilla (ES5, patron UMD comme `srs-core.js`), `node:test` pour les tests unitaires, localStorage via le `Store` existant, PWA service worker (`sw.js`).

## Global Constraints

- **Style module** : UMD ES5 exactement comme `srs-core.js` / `parcours-core.js` (pas de `let`/`const`/arrow dans les fichiers `*-core.js` livrés ; les fichiers de test peuvent utiliser ES moderne comme les tests existants).
- **Ordre des piliers figé** : `["study","read","listen","speak","journal"]`.
- **XP** : `+10` par pilier validé, `+20` bonus si les 5 piliers d'un jour sont faits (journée pleine = 70 XP).
- **Cible** : `TARGET_DAYS = 75` ; paliers de badges `MILESTONES = [7, 21, 50, 75]` ; badge XP à `1000`.
- **Date** : toujours réutiliser `todayISO()` existant ([index.html:986](../../../index.html#L986)) côté UI ; côté core, dates au format ISO `yyyy-mm-dd`.
- **Règle souple** : rater un jour casse la streak mais ne remet pas la progression /75 à zéro ; « /75 » compte les **journées pleines**, pas les jours calendaires.
- **Tests** : `npm test` (= `node --test`) ; les fichiers de test vivent dans `tests/`.

---

## File Structure

- **Create** `challenge-core.js` — logique pure du challenge (état, piliers, XP, streak, progression, badges). Aucune dépendance DOM.
- **Create** `tests/challenge-core.test.js` — tests unitaires du module.
- **Modify** `index.html` — chargement du script, état + `markPillar`, onglet Challenge (nav + panel + rendu), hooks auto, mini-encart accueil, CSS.
- **Modify** `sw.js` — bump du cache + ajout de `challenge-core.js` (et `parcours-core.js`, absent par oubli) au précache.

---

## Task 1: Module `challenge-core.js` (moteur pur + tests)

**Files:**
- Create: `challenge-core.js`
- Test: `tests/challenge-core.test.js`

**Interfaces:**
- Consumes: rien (module autonome).
- Produces (API globale `window.ChallengeCore` / `module.exports`) :
  - `PILLARS: string[]`, `XP_PER_PILLAR: number`, `FULL_DAY_BONUS: number`, `TARGET_DAYS: number`, `MILESTONES: number[]`, `XP_BADGE: number`
  - `emptyDay() -> {study,read,listen,speak,journal}` (tous `false`)
  - `dayPillars(state, dayISO) -> day` (défaut tout-false)
  - `setPillar(state, dayISO, pillar, value?) -> newState` (pur, ne mute pas ; `value` défaut `true` ; pose `startDate` si absent ; ignore un pilier inconnu)
  - `dayCount(day) -> number`, `dayComplete(day) -> boolean`
  - `xpForDay(day) -> number`, `totalXp(state) -> number`
  - `daysCompleted(state) -> number`, `progress(state) -> {done,total,pct}` (done plafonné à 75)
  - `addDays(iso, n) -> iso`
  - `streak(state, todayISO) -> number`, `ringsForToday(state, todayISO) -> day`
  - `badgesUnlocked(state) -> Array<number|"xp">`

- [ ] **Step 1: Écrire le test qui échoue**

Create `tests/challenge-core.test.js` :

```js
"use strict";
const test = require("node:test");
const assert = require("node:assert");
const CH = require("../challenge-core.js");

const T = "2026-07-21";
function full(){ return { study:true, read:true, listen:true, speak:true, journal:true }; }

test("constantes exposées", () => {
  assert.deepStrictEqual(CH.PILLARS, ["study","read","listen","speak","journal"]);
  assert.strictEqual(CH.XP_PER_PILLAR, 10);
  assert.strictEqual(CH.FULL_DAY_BONUS, 20);
  assert.strictEqual(CH.TARGET_DAYS, 75);
  assert.deepStrictEqual(CH.MILESTONES, [7,21,50,75]);
});

test("setPillar: crée le jour, ne mute pas l'entrée, pose startDate", () => {
  const s0 = { startDate:null, days:{} };
  const s1 = CH.setPillar(s0, T, "study", true);
  assert.strictEqual(s0.days[T], undefined);
  assert.strictEqual(s1.days[T].study, true);
  assert.strictEqual(s1.startDate, T);
});

test("setPillar: value défaut true, décoche avec false, ignore inconnu", () => {
  let s = CH.setPillar({startDate:T,days:{}}, T, "speak");
  assert.strictEqual(s.days[T].speak, true);
  s = CH.setPillar(s, T, "speak", false);
  assert.strictEqual(s.days[T].speak, false);
  const s2 = CH.setPillar({startDate:T,days:{}}, T, "bogus", true);
  assert.strictEqual(s2.days[T], undefined);
});

test("dayPillars: défaut tout-false si jour absent", () => {
  assert.deepStrictEqual(CH.dayPillars({days:{}}, T), CH.emptyDay());
});

test("dayCount / dayComplete", () => {
  assert.strictEqual(CH.dayCount({study:true,read:true}), 2);
  assert.strictEqual(CH.dayComplete(full()), true);
  assert.strictEqual(CH.dayComplete({study:true}), false);
});

test("xpForDay: 10/pilier, +20 si complet", () => {
  assert.strictEqual(CH.xpForDay({study:true,read:true}), 20);
  assert.strictEqual(CH.xpForDay(full()), 70);
  assert.strictEqual(CH.xpForDay(CH.emptyDay()), 0);
});

test("totalXp: somme multi-jours", () => {
  const s = { days: { "2026-07-21": full(), "2026-07-22": {study:true,read:true} } };
  assert.strictEqual(CH.totalXp(s), 90);
});

test("daysCompleted: compte les pleines, ignore partielles", () => {
  const s = { days: { a: full(), b: {study:true}, c: full() } };
  assert.strictEqual(CH.daysCompleted(s), 2);
});

test("progress: done/total/pct, plafond 75", () => {
  const days = {};
  for (let i=0;i<80;i++) days["d"+i] = full();
  const p = CH.progress({days});
  assert.strictEqual(p.done, 75);
  assert.strictEqual(p.total, 75);
  assert.strictEqual(p.pct, 100);
});

test("streak: jours pleins consécutifs finissant aujourd'hui", () => {
  const s = { days: { "2026-07-19": full(), "2026-07-20": full(), "2026-07-21": full() } };
  assert.strictEqual(CH.streak(s, "2026-07-21"), 3);
});

test("streak: tolérance aujourd'hui vide mais hier plein", () => {
  const s = { days: { "2026-07-20": full() } };
  assert.strictEqual(CH.streak(s, "2026-07-21"), 1);
});

test("streak: un trou coupe la série", () => {
  const s = { days: { "2026-07-18": full(), "2026-07-20": full(), "2026-07-21": full() } };
  assert.strictEqual(CH.streak(s, "2026-07-21"), 2);
});

test("streak: journée partielle aujourd'hui -> repart d'hier plein", () => {
  const s = { days: { "2026-07-20": full(), "2026-07-21": {study:true} } };
  assert.strictEqual(CH.streak(s, "2026-07-21"), 1);
});

test("streak: 0 si rien", () => {
  assert.strictEqual(CH.streak({days:{}}, "2026-07-21"), 0);
});

test("ringsForToday = piliers du jour", () => {
  const s = { days: { "2026-07-21": {study:true} } };
  assert.strictEqual(CH.ringsForToday(s, "2026-07-21").study, true);
  assert.strictEqual(CH.ringsForToday(s, "2026-07-21").read, false);
});

test("badgesUnlocked: paliers de journées franchis", () => {
  const d7 = {}; for (let i=0;i<7;i++) d7["d"+i]=full();
  assert.deepStrictEqual(CH.badgesUnlocked({days:d7}), [7]);
  const d21 = {}; for (let i=0;i<21;i++) d21["d"+i]=full();
  assert.deepStrictEqual(CH.badgesUnlocked({days:d21}), [7,21]);
});

test("badgesUnlocked: badge xp à 1000 XP", () => {
  const d = {}; for (let i=0;i<15;i++) d["d"+i]=full(); // 15*70 = 1050 XP, 15 journées
  const b = CH.badgesUnlocked({days:d});
  assert.ok(b.indexOf(7) >= 0);
  assert.ok(b.indexOf("xp") >= 0);
});
```

- [ ] **Step 2: Lancer le test pour vérifier qu'il échoue**

Run: `npm test`
Expected: FAIL — `Cannot find module '../challenge-core.js'`.

- [ ] **Step 3: Écrire le module**

Create `challenge-core.js` :

```js
(function (root, factory) {
  "use strict";
  var mod = factory();
  if (typeof module !== "undefined" && module.exports) module.exports = mod;
  else root.ChallengeCore = mod;
})(typeof self !== "undefined" ? self : this, function () {
  "use strict";

  var PILLARS = ["study", "read", "listen", "speak", "journal"];
  var XP_PER_PILLAR = 10;
  var FULL_DAY_BONUS = 20;
  var TARGET_DAYS = 75;
  var MILESTONES = [7, 21, 50, 75];
  var XP_BADGE = 1000;

  function emptyDay() {
    return { study: false, read: false, listen: false, speak: false, journal: false };
  }

  function dayPillars(state, dayISO) {
    var src = state && state.days && state.days[dayISO];
    var out = emptyDay();
    if (src) for (var i = 0; i < PILLARS.length; i++) out[PILLARS[i]] = !!src[PILLARS[i]];
    return out;
  }

  function setPillar(state, dayISO, pillar, value) {
    if (PILLARS.indexOf(pillar) < 0) return state;
    var next = { startDate: (state && state.startDate) || dayISO, days: {} };
    if (state && state.days) {
      for (var k in state.days) {
        if (Object.prototype.hasOwnProperty.call(state.days, k)) {
          next.days[k] = {};
          for (var p in state.days[k]) {
            if (Object.prototype.hasOwnProperty.call(state.days[k], p)) next.days[k][p] = state.days[k][p];
          }
        }
      }
    }
    if (!next.days[dayISO]) next.days[dayISO] = emptyDay();
    next.days[dayISO][pillar] = (value === undefined) ? true : !!value;
    return next;
  }

  function dayCount(day) {
    if (!day) return 0;
    var n = 0;
    for (var i = 0; i < PILLARS.length; i++) if (day[PILLARS[i]]) n++;
    return n;
  }

  function dayComplete(day) { return dayCount(day) === PILLARS.length; }

  function xpForDay(day) {
    var c = dayCount(day);
    return c * XP_PER_PILLAR + (c === PILLARS.length ? FULL_DAY_BONUS : 0);
  }

  function totalXp(state) {
    var sum = 0, days = (state && state.days) || {};
    for (var k in days) if (Object.prototype.hasOwnProperty.call(days, k)) sum += xpForDay(days[k]);
    return sum;
  }

  function daysCompleted(state) {
    var n = 0, days = (state && state.days) || {};
    for (var k in days) if (Object.prototype.hasOwnProperty.call(days, k) && dayComplete(days[k])) n++;
    return n;
  }

  function progress(state) {
    var done = daysCompleted(state);
    var capped = done > TARGET_DAYS ? TARGET_DAYS : done;
    return { done: capped, total: TARGET_DAYS, pct: Math.round(capped / TARGET_DAYS * 100) };
  }

  function addDays(iso, n) {
    var p = String(iso).split("-");
    var d = new Date(Date.UTC(+p[0], (+p[1]) - 1, +p[2]));
    d.setUTCDate(d.getUTCDate() + n);
    var y = d.getUTCFullYear();
    var m = ("0" + (d.getUTCMonth() + 1)).slice(-2);
    var day = ("0" + d.getUTCDate()).slice(-2);
    return y + "-" + m + "-" + day;
  }

  function streak(state, todayISO) {
    var days = (state && state.days) || {};
    function isFull(iso) { return dayComplete(days[iso]); }
    var cursor = todayISO;
    if (!isFull(cursor)) {
      var y = addDays(todayISO, -1);
      if (!isFull(y)) return 0;
      cursor = y;
    }
    var s = 0;
    while (isFull(cursor)) { s++; cursor = addDays(cursor, -1); }
    return s;
  }

  function ringsForToday(state, todayISO) { return dayPillars(state, todayISO); }

  function badgesUnlocked(state) {
    var done = daysCompleted(state);
    var out = [];
    for (var i = 0; i < MILESTONES.length; i++) if (done >= MILESTONES[i]) out.push(MILESTONES[i]);
    if (totalXp(state) >= XP_BADGE) out.push("xp");
    return out;
  }

  return {
    PILLARS: PILLARS, XP_PER_PILLAR: XP_PER_PILLAR, FULL_DAY_BONUS: FULL_DAY_BONUS,
    TARGET_DAYS: TARGET_DAYS, MILESTONES: MILESTONES, XP_BADGE: XP_BADGE,
    emptyDay: emptyDay, dayPillars: dayPillars, setPillar: setPillar,
    dayCount: dayCount, dayComplete: dayComplete, xpForDay: xpForDay, totalXp: totalXp,
    daysCompleted: daysCompleted, progress: progress, addDays: addDays,
    streak: streak, ringsForToday: ringsForToday, badgesUnlocked: badgesUnlocked
  };
});
```

- [ ] **Step 4: Lancer les tests pour vérifier qu'ils passent**

Run: `npm test`
Expected: PASS — tous les tests `challenge-core` verts (les tests existants restent verts).

- [ ] **Step 5: Commit**

```bash
git add challenge-core.js tests/challenge-core.test.js
git commit -m "feat: challenge-core (moteur du Challenge 75) + tests"
```

---

## Task 2: Onglet « Challenge » — état, markPillar, cockpit, toggles manuels

**Files:**
- Modify: `index.html` (script include `809` ; nav `264` ; panneau avant `435` ; CSS dans `<style>` ; bloc JS près du bloc PARCOURS `~1043`)

**Interfaces:**
- Consumes: `window.ChallengeCore` (Task 1) ; helpers existants `$`, `$$`, `Store`, `todayISO()`, `activateTab()`.
- Produces (fonctions globales dans le script principal, utilisées par Task 3 & 4) :
  - `markPillar(pillar, value?)` — pose un pilier du jour, persiste, re-rend, déclenche le toast badge
  - `renderChallenge()` — rend le cockpit (+ grille & badges ajoutés en Task 4)
  - `refreshChallengeMini()` — rend le mini-encart accueil (mount ajouté en Task 5 ; no-op si absent)
  - variables : `challenge` (état), `BADGE_DEFS`, `CH_LABELS`, `CH_AUTO`

> Cette tâche se vérifie **manuellement dans le navigateur** (le projet n'a pas de harnais de test DOM ; cf. specs précédents).

- [ ] **Step 1: Charger le module**

Modify `index.html` — après la ligne `<script src="parcours-core.js"></script>` ([index.html:809](../../../index.html#L809)), ajouter :

```html
<script src="challenge-core.js"></script>
```

- [ ] **Step 2: Ajouter l'onglet dans la nav**

Modify `index.html` — juste après `<button data-tab="parcours">Parcours</button>` ([index.html:264](../../../index.html#L264)), ajouter :

```html
      <button data-tab="challenge">Challenge</button>
```

- [ ] **Step 3: Ajouter le panneau de l'onglet**

Modify `index.html` — insérer un nouveau `<section>` juste avant `<section class="panel" id="tab-grammaire">` ([index.html:435](../../../index.html#L435)) :

```html
<section class="panel" id="tab-challenge">
  <div class="block-head"><span class="num">75</span><h2>Challenge 75</h2></div>
  <p class="intro">5 piliers par jour. Trois se cochent tout seuls quand tu bosses dans l'app (⚡), deux sont à cocher à la main. Ne casse pas la série 🔥</p>
  <div id="ch-cockpit" class="ch-cockpit"></div>
  <div id="ch-grid" class="ch-grid"></div>
  <div id="ch-badges" class="ch-badges"></div>
</section>
```

- [ ] **Step 4: Ajouter le CSS**

Modify `index.html` — ajouter à la fin du bloc `<style>` (avant `</style>`) :

```css
/* --- Challenge 75 --- */
.ch-cockpit{background:#fff;border:1px solid var(--line);border-radius:14px;padding:16px;margin:14px 0}
.ch-stats{display:flex;gap:18px;align-items:baseline;flex-wrap:wrap;margin-bottom:14px}
.ch-streak{font-size:26px;font-weight:800;color:var(--ink)}
.ch-streak span{font-size:14px;font-weight:600;color:var(--muted)}
.ch-xp{font-size:15px;font-weight:700;color:var(--ocean)}
.ch-xp span,.ch-total span{font-size:12px;font-weight:600;color:var(--muted)}
.ch-total{font-size:13px;color:var(--muted);margin-left:auto}
.ch-rings{display:flex;gap:10px;flex-wrap:wrap}
.ch-ring{display:flex;flex-direction:column;align-items:center;gap:6px;background:none;border:none;cursor:pointer;padding:4px;font-family:inherit}
.ch-ring[disabled]{cursor:default}
.ch-ring-dot{width:44px;height:44px;border-radius:50%;border:4px solid var(--line-strong);transition:.2s}
.ch-ring.on .ch-ring-dot{border-color:var(--ocean);background:var(--ocean)}
.ch-ring-lbl{font-size:11px;font-weight:700;color:var(--muted)}
.ch-ring.on .ch-ring-lbl{color:var(--ink)}
.ch-manual-hint{font-size:12px;color:var(--muted);margin-top:12px}
.ch-manual-hint a{color:var(--ocean);font-weight:600;text-decoration:none}
.ch-grid{margin:14px 0}
.ch-grid-head{font-size:14px;font-weight:700;color:var(--ink);margin-bottom:8px}
.ch-cells{display:grid;grid-template-columns:repeat(15,1fr);gap:4px;max-width:420px}
.ch-cell{aspect-ratio:1;border-radius:4px;background:#eef1ee;border:1px solid var(--line)}
.ch-cell.partial{background:var(--turmeric)}
.ch-cell.full{background:var(--ocean)}
.ch-cell.milestone{outline:2px solid var(--turmeric);outline-offset:1px}
.ch-badges{margin:14px 0}
.ch-badges-head{font-size:14px;font-weight:700;color:var(--ink);margin-bottom:8px}
.ch-badges-row{display:flex;gap:12px;flex-wrap:wrap}
.ch-badge{display:flex;flex-direction:column;align-items:center;gap:4px;opacity:.35;filter:grayscale(1);width:66px;text-align:center}
.ch-badge.on{opacity:1;filter:none}
.ch-badge-emo{font-size:26px}
.ch-badge-nm{font-size:11px;font-weight:700;color:var(--muted)}
.ch-toast{position:fixed;left:50%;bottom:24px;transform:translateX(-50%) translateY(20px);background:var(--ink);color:#fff;padding:12px 18px;border-radius:12px;font-size:14px;box-shadow:var(--shadow);opacity:0;transition:.3s;z-index:60}
.ch-toast.show{opacity:1;transform:translateX(-50%) translateY(0)}
/* mini-encart accueil */
.ch-mini{display:inline-flex;align-items:center;gap:8px;background:#fff;border:1px solid var(--line);border-radius:12px;padding:8px 12px;cursor:pointer;font-family:inherit;margin-bottom:10px}
.ch-mini-streak{font-weight:800;color:var(--ink);font-size:14px}
.ch-mini-dot{width:12px;height:12px;border-radius:50%;border:2px solid var(--line-strong)}
.ch-mini-dot.on{border-color:var(--ocean);background:var(--ocean)}
.ch-mini-xp{font-size:12px;font-weight:700;color:var(--ocean)}
```

- [ ] **Step 5: Ajouter le bloc JS du Challenge**

Modify `index.html` — insérer ce bloc juste après le bloc `/* ---------- PARCOURS (fil guidé) ---------- */` (après le `lessonsReady = ...` qui se termine [index.html:1043](../../../index.html#L1043) et suivantes ; le placer avant le prochain commentaire de section) :

```js
/* ---------- CHALLENGE 75 ---------- */
var CH = window.ChallengeCore;
var challenge = { startDate: null, days: {} };
var CH_LABELS = { study:"Étudier", read:"Lire", listen:"Écouter", speak:"Parler", journal:"Journal" };
var CH_AUTO   = { study:true, read:true, listen:true, speak:false, journal:false };
var BADGE_DEFS = [
  { key: 7,  emoji: "🏝️", name: "Bali" },
  { key: 21, emoji: "🌋", name: "Krakatau" },
  { key: 50, emoji: "🐉", name: "Komodo" },
  { key: 75, emoji: "🏆", name: "Merdeka" },
  { key: "xp", emoji: "⭐", name: "1000 XP" }
];

function saveChallenge(){ Store.set("challenge", JSON.stringify(challenge)); }

function showBadgeToast(key){
  var def = BADGE_DEFS.filter(function(b){ return b.key === key; })[0];
  if(!def) return;
  var t = document.createElement("div");
  t.className = "ch-toast";
  t.innerHTML = def.emoji + ' Badge débloqué : <b>' + def.name + '</b> !';
  document.body.appendChild(t);
  setTimeout(function(){ t.classList.add("show"); }, 20);
  setTimeout(function(){
    t.classList.remove("show");
    setTimeout(function(){ if(t.parentNode) t.parentNode.removeChild(t); }, 300);
  }, 3200);
}

function markPillar(pillar, value){
  if(!CH || CH.PILLARS.indexOf(pillar) < 0) return;
  var today = todayISO();
  var before = CH.badgesUnlocked(challenge);
  challenge = CH.setPillar(challenge, today, pillar, value === undefined ? true : value);
  saveChallenge();
  renderChallenge();
  refreshChallengeMini();
  CH.badgesUnlocked(challenge).filter(function(b){ return before.indexOf(b) < 0; }).forEach(showBadgeToast);
}

function ringHTML(pillar, on){
  var auto = CH_AUTO[pillar];
  return '<button class="ch-ring'+(on?' on':'')+(auto?' auto':'')+'" data-pillar="'+pillar+'"'+(auto?' disabled':'')+'>'
    + '<span class="ch-ring-dot"></span>'
    + '<span class="ch-ring-lbl">'+CH_LABELS[pillar]+(auto?' ⚡':'')+'</span>'
    + '</button>';
}

function renderChallenge(){
  if(!CH) return;
  var today = todayISO();
  var rings = CH.ringsForToday(challenge, today);
  var day = CH.dayPillars(challenge, today);

  var cp = document.getElementById("ch-cockpit");
  if(cp){
    cp.innerHTML =
      '<div class="ch-stats">'
      + '<div class="ch-streak">🔥 '+CH.streak(challenge, today)+'<span> j</span></div>'
      + '<div class="ch-xp">'+CH.xpForDay(day)+' / 70 XP<span> aujourd\'hui</span></div>'
      + '<div class="ch-total">'+CH.totalXp(challenge)+' XP<span> total</span></div>'
      + '</div>'
      + '<div class="ch-rings">'+CH.PILLARS.map(function(p){ return ringHTML(p, rings[p]); }).join('')+'</div>'
      + '<div class="ch-manual-hint">Parler / Journal : à cocher à la main. <a href="#" id="ch-journal-link">→ Écrire dans le Carnet</a></div>';
    $$(".ch-ring:not(.auto)", cp).forEach(function(b){
      b.addEventListener("click", function(){
        var p = b.getAttribute("data-pillar");
        markPillar(p, !CH.dayPillars(challenge, todayISO())[p]);
      });
    });
    var jl = document.getElementById("ch-journal-link");
    if(jl) jl.addEventListener("click", function(e){ e.preventDefault(); activateTab("carnet"); });
  }
  renderChallengeGrid();
  renderChallengeBadges();
}

/* Étoffés en Task 4 ; définis ici en no-op pour éviter les erreurs. */
function renderChallengeGrid(){}
function renderChallengeBadges(){}
function refreshChallengeMini(){}

var challengeReady = Store.get("challenge").then(function(v){
  if(v){ try{ challenge = JSON.parse(v) || { startDate:null, days:{} }; }catch(e){ challenge = { startDate:null, days:{} }; } }
  renderChallenge();
  refreshChallengeMini();
});
```

- [ ] **Step 6: Vérifier dans le navigateur**

Ouvrir `index.html` (via le serveur du projet). Vérifier :
- L'onglet « Challenge » apparaît dans la nav, à côté de « Parcours », et l'ouvre.
- Le cockpit affiche 🔥 0 j, 0 / 70 XP, 0 XP total, et 5 anneaux (Étudier ⚡, Lire ⚡, Écouter ⚡, Parler, Journal).
- Cliquer sur l'anneau **Parler** le remplit (bleu) et passe l'XP du jour à 10 ; recliquer le vide (retour à 0). Idem **Journal**.
- Les anneaux ⚡ (Étudier/Lire/Écouter) ne sont pas cliquables.
- Le lien « → Écrire dans le Carnet » ouvre l'onglet Carnet.
- Recharger la page conserve l'état coché (persistance Store).

- [ ] **Step 7: Commit**

```bash
git add index.html
git commit -m "feat: onglet Challenge 75 (cockpit, anneaux, toggles manuels, persistance)"
```

---

## Task 3: Hooks auto — Étudier / Lire / Écouter

**Files:**
- Modify: `index.html` — `srsUpdate` ([index.html:993](../../../index.html#L993)), `lessonDone` ([index.html:1024](../../../index.html#L1024)), handler du quiz écoute ([index.html:1441](../../../index.html#L1441))

**Interfaces:**
- Consumes: `markPillar(pillar)` (Task 2).
- Produces: rien de nouveau (branche des appels existants).

> Vérification **manuelle navigateur**.

- [ ] **Step 1: Brancher Study**

Dans `lessonDone(id, done)` ([index.html:1024](../../../index.html#L1024)), après `Store.set("lessons", JSON.stringify(lessonsState));` et avant les rafraîchissements, ajouter :

```js
  if(done && typeof markPillar === "function") markPillar("study");
```

- [ ] **Step 2: Brancher Read**

Dans `srsUpdate(id, knewIt)` ([index.html:993](../../../index.html#L993)), après `refreshSrsUI();`, ajouter :

```js
  if(typeof markPillar === "function") markPillar("read");
```

- [ ] **Step 3: Brancher Listen**

Dans le handler de réponse du quiz écoute ([index.html:1440-1447](../../../index.html#L1440-L1447)), juste après l'appel existant `srsUpdate(current.id, op.fr===current.fr);` ([index.html:1445](../../../index.html#L1445)), ajouter :

```js
        if(typeof markPillar === "function") markPillar("listen");
```

(Ce point distingue « quiz écoute terminé » de « carte révisée » : `srsUpdate` seul coche Lire, ce hook coche en plus Écouter.)

- [ ] **Step 4: Vérifier dans le navigateur**

- Aller au Parcours, marquer une leçon « fait » → l'anneau **Étudier** du Challenge se coche (XP +10).
- Réviser une carte SRS (flashcards / quiz vocab) → l'anneau **Lire** se coche.
- Faire une question du quiz d'écoute (Entraînement) → les anneaux **Lire** ET **Écouter** se cochent.
- Cocher aussi Parler + Journal → 5/5 : XP du jour = 70, une case de la grille passera pleine (visible après Task 4), streak = 1.

- [ ] **Step 5: Commit**

```bash
git add index.html
git commit -m "feat: hooks auto du Challenge (etudier/lire/ecouter depuis l'activite)"
```

---

## Task 4: Grille 75 + badges + toast

**Files:**
- Modify: `index.html` — remplacer les no-op `renderChallengeGrid()` et `renderChallengeBadges()` du bloc CHALLENGE (Task 2).

**Interfaces:**
- Consumes: `CH` (`daysCompleted`, `dayPillars`, `dayCount`, `progress`, `MILESTONES`, `badgesUnlocked`), `challenge`, `BADGE_DEFS`, `todayISO()`.
- Produces: rien de nouveau.

> Vérification **manuelle navigateur**.

- [ ] **Step 1: Implémenter la grille**

Remplacer `function renderChallengeGrid(){}` par :

```js
function renderChallengeGrid(){
  var grid = document.getElementById("ch-grid");
  if(!grid || !CH) return;
  var today = todayISO();
  var prog = CH.progress(challenge);
  var done = CH.daysCompleted(challenge);
  var todayCount = CH.dayCount(CH.dayPillars(challenge, today));
  var todayFull = todayCount === CH.PILLARS.length;
  var partialIdx = (!todayFull && todayCount > 0) ? done : -1; // prochaine case = jour en cours
  var cells = "";
  for(var i=0;i<CH.TARGET_DAYS;i++){
    var cls = "ch-cell";
    if(i < Math.min(done, CH.TARGET_DAYS)) cls += " full";
    else if(i === partialIdx && partialIdx < CH.TARGET_DAYS) cls += " partial";
    if(CH.MILESTONES.indexOf(i+1) >= 0) cls += " milestone";
    cells += '<div class="'+cls+'" title="Jour '+(i+1)+'"></div>';
  }
  grid.innerHTML = '<div class="ch-grid-head">'+prog.done+' / '+CH.TARGET_DAYS+' journées pleines · '+prog.pct+'%</div>'
    + '<div class="ch-cells">'+cells+'</div>';
}
```

- [ ] **Step 2: Implémenter les badges**

Remplacer `function renderChallengeBadges(){}` par :

```js
function renderChallengeBadges(){
  var mount = document.getElementById("ch-badges");
  if(!mount || !CH) return;
  var unlocked = CH.badgesUnlocked(challenge);
  mount.innerHTML = '<div class="ch-badges-head">Badges</div><div class="ch-badges-row">'
    + BADGE_DEFS.map(function(b){
        var on = unlocked.indexOf(b.key) >= 0;
        return '<div class="ch-badge'+(on?' on':'')+'">'
          + '<span class="ch-badge-emo">'+b.emoji+'</span>'
          + '<span class="ch-badge-nm">'+b.name+'</span></div>';
      }).join('')
    + '</div>';
}
```

- [ ] **Step 3: Vérifier dans le navigateur**

- Onglet Challenge : la grille de 75 cases s'affiche ; les cases 7/21/50/75 ont un liseré ; le compteur « 0 / 75 journées pleines · 0% ».
- Compléter une journée (5 piliers) → 1re case pleine (bleue) + compteur « 1 / 75 ».
- Une journée entamée mais pas pleine → case suivante en « partielle » (jaune/turmeric).
- Les badges sont grisés au départ.
- *Test rapide du toast/badge sans attendre 7 jours* : dans la console, injecter 7 journées pleines puis re-rendre —
  ```js
  for (let i=0;i<7;i++){ const d="2026-06-"+String(i+1).padStart(2,"0"); challenge=ChallengeCore.setPillar(challenge,d,"study",true); challenge=ChallengeCore.setPillar(challenge,d,"read",true); challenge=ChallengeCore.setPillar(challenge,d,"listen",true); challenge=ChallengeCore.setPillar(challenge,d,"speak",true); challenge=ChallengeCore.setPillar(challenge,d,"journal",true);} markPillar("study");
  ```
  → le badge 🏝️ Bali devient coloré et un toast « Badge débloqué : Bali ! » apparaît. Recharger ensuite pour repartir d'un état propre (ou nettoyer via `Store`).

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "feat: grille 75 jours + badges paliers + toast du Challenge"
```

---

## Task 5: Mini-encart accueil + service worker

**Files:**
- Modify: `index.html` — mount dans `#tab-home` ([index.html:292](../../../index.html#L292)) ; implémenter `refreshChallengeMini()` ; brancher le clic.
- Modify: `sw.js` — bump cache + précache.

**Interfaces:**
- Consumes: `CH`, `challenge`, `CH_LABELS`, `todayISO()`, `activateTab()`.
- Produces: rien de nouveau.

> Vérification **manuelle navigateur**.

- [ ] **Step 1: Ajouter le mount dans l'accueil**

Modify `index.html` — juste après l'ouverture `<section class="panel on" id="tab-home">` ([index.html:292](../../../index.html#L292)), ajouter :

```html
  <button id="ch-mini" class="ch-mini" aria-label="Ouvrir le Challenge 75"></button>
```

- [ ] **Step 2: Implémenter `refreshChallengeMini` + clic**

Modify `index.html` — remplacer le no-op `function refreshChallengeMini(){}` (bloc CHALLENGE, Task 2) par :

```js
function refreshChallengeMini(){
  var mount = document.getElementById("ch-mini");
  if(!mount || !CH) return;
  var today = todayISO();
  var rings = CH.ringsForToday(challenge, today);
  var dots = CH.PILLARS.map(function(p){
    return '<span class="ch-mini-dot'+(rings[p]?' on':'')+'" title="'+CH_LABELS[p]+'"></span>';
  }).join('');
  mount.innerHTML = '<span class="ch-mini-streak">🔥 '+CH.streak(challenge, today)+'</span>'
    + dots
    + '<span class="ch-mini-xp">'+CH.xpForDay(CH.dayPillars(challenge, today))+'/70</span>';
  if(!mount.dataset.bound){
    mount.addEventListener("click", function(){ activateTab("challenge"); });
    mount.dataset.bound = "1";
  }
}
```

- [ ] **Step 3: Bump du service worker**

Modify `sw.js` :
- Ligne 3, remplacer `var CACHE = "bahasa-v4";` par `var CACHE = "bahasa-v5";`
- Dans `SHELL`, après `"./srs-core.js",`, ajouter les deux lignes :

```js
  "./parcours-core.js",
  "./challenge-core.js",
```

(`parcours-core.js` manquait du précache — corrigé au passage pour le hors-ligne.)

- [ ] **Step 4: Vérifier dans le navigateur**

- Accueil : le mini-encart affiche 🔥 <streak>, 5 pastilles (pleines selon les piliers du jour), et « x/70 ». Cliquer dessus ouvre l'onglet Challenge.
- Cocher un pilier dans l'onglet Challenge → le mini-encart de l'accueil reflète le changement.
- Onglet Application (DevTools) : après reload, le service worker actif est `bahasa-v5` et `challenge-core.js` est dans le cache ; couper le réseau et recharger → l'app (dont le Challenge) fonctionne hors-ligne.

- [ ] **Step 5: Commit**

```bash
git add index.html sw.js
git commit -m "feat: encart Challenge sur l'accueil + bump cache SW (bahasa-v5)"
```

---

## Self-Review (fait pendant la rédaction)

- **Couverture du spec :** modèle d'état (T1), core pur + toutes fonctions listées (T1), hooks Study/Read/Listen (T3) & toggles Speak/Journal (T2), streak souple (T1), XP 10+20 (T1), grille 75 + partielle + jalons (T4), badges + toast (T2/T4), onglet dédié (T2), mini-encart accueil (T5), style via variables existantes (T2), tests unitaires (T1), risques (parse try/catch T2, hook Listen isolé T3, `todayISO` partout, SW bump T5) — tous couverts.
- **Placeholders :** les `renderChallengeGrid/Badges/refreshChallengeMini` sont volontairement des no-op en T2 avec code complet fourni en T4/T5 (pas de « TODO » vague — le code réel est donné plus loin dans le même plan).
- **Cohérence des types :** noms alignés entre tâches — `markPillar(pillar, value?)`, `challenge`, `BADGE_DEFS` (clés `number|"xp"`), `CH_AUTO`, `CH.PILLARS/streak/xpForDay/dayPillars/progress/daysCompleted/badgesUnlocked`. La comparaison badge utilise `indexOf` sur les mêmes clés produites par `badgesUnlocked`.
